This folder is used via the post build step of the SQLiteTools project to cache the SQLite DLLs.

The pre build step of the LcmsNetSQLiteToolsUnitTests project copies these files to the target folder of the build.
