<UserControl
    x:Class="LcmsNet.sampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LcmsNet"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Path=Samples}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn x:Name="Queued_Checkbox" Header="Checkbox" />

                <DataGridTextColumn x:Name="Status" Header="Status" />

                <DataGridTextColumn x:Name="SequenceNumber" Header="Seq #" />

                <DataGridTextColumn x:Name="ColumnNumber" Header="Col #" />
                <DataGridTextColumn x:Name="DatasetName" Header="Dataset Name" />
                <DataGridComboBoxColumn Header="PAL Tray" />
                <DataGridTextColumn Header="PAL Vial" />
                <DataGridTextColumn Header="Volume" />
                <DataGridComboBoxColumn Header="LC Method" />
                <DataGridComboBoxColumn Header="Dataset Type" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
