<UserControl
    x:Class="LcmsNet.WPFControls.Views.columnView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LcmsNet.WPFControls.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Path=Samples}">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="SequenceNumber" Header="Seq" />
                <DataGridTextColumn x:Name="DatasetName" Header="Dataset Name" />
                <DataGridComboBoxColumn Header="LC Method" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
