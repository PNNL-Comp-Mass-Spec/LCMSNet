<UserControl x:Class="ASUTGen.Devices.Valves.IDEXValveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ASUTGen.Devices.Valves"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type local:IDEXValveViewModel}, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Margin="3" Content="Inject Failure" Command="{Binding InjectFailureCommand}" />
        <Button Grid.Row="1" Margin="3" Content="Inject Status" Command="{Binding InjectStatusCommand}" />
    </Grid>
</UserControl>
