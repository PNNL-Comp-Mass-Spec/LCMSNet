These are projects that are used by LCMSNet or other projects in this repository, that are not SDKs, executables, or full plugins.

An example:
A library that is used by a single plugin and by one or two separate executables, that has a dependency on one of the SDKs.
